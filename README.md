#This is a shitty but yet functionnal brainf*ck interpreter written in C

#For compilation process, under Linux type (being at project root):
```gcc src/main.c src/lib.c -o build/bf```

#For running type:
```./build/bf```